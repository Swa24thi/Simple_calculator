# A Simple Calculator

Using Streamlit for UI, designed a simple calculator which handles the edge cases too.

## Setup

Install Streamlit package on the device and add it in pycharm as well.

command to RUN : streamlit run <file_name>
